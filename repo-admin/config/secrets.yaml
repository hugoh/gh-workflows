# Secret name -> target repo list. Values live sops-encrypted in
# secrets.enc.yaml (not committed until you create it -- see README.md).
TAP_GITHUB_TOKEN:
  repos: [hrd, jj-trim, netcheck, tmhi-cli]
